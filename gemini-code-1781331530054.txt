You are an Elite Software Architect and Master Prompt Engineer. 

I am building a gamified task and hobby project management application called **QuestLog**. I need you to break down our development blueprint into highly explicit, sequential, and context-isolated prompt steps that I can copy-paste one by one into our coding agents (such as Claude 3.5 Sonnet or Claude Haiku).

Your goal is to output a clear list of developer prompts based on the architecture and sprint plan outlined below.

---

### Technical Stack (Fully Self-Hosted)
- **Frontend & Server:** Next.js 14+ (App Router, Standalone Mode Output)
- **Database:** PostgreSQL 16 (Containerized via persistent Docker volume)
- **ORM:** Prisma ORM (Type-safe client generation & schema management)
- **Authentication:** NextAuth.js (Local DB credentials, bcryptjs password hashing)
- **State Management:** Zustand (Lightweight store for client-side optimistic UI updates)
- **UI Components:** Tailwind CSS + shadcn/ui primitives

---

### Folder & Workspace Structure
questlog/
├── .gitignore
├── docker-compose.yml
├── Dockerfile
├── package.json
├── tsconfig.json
├── prisma/
│   └── schema.prisma
└── src/
    ├── app/
    │   ├── layout.tsx
    │   ├── page.tsx
    │   ├── api/auth/[...nextauth]/route.ts
    │   └── actions/projects.ts
    ├── components/
    │   └── ui/ (Button, Card, Checkbox, Progress)
    ├── lib/
    │   ├── prisma.ts
    │   └── utils.ts
    └── store/
        └── useProjectStore.ts

---

### Your Instructions
Generate a series of structured prompts for the coding agent. For EACH task listed below, write out a complete prompt template that:
1. **Defines the Role/Context:** Tells the agent exactly what phase of development they are in.
2. **Lists Explicit Sub-steps:** Breaks down the specific code, files, configurations, or dependencies to initialize.
3. **Imposes Strict Guardrails:** Forces type safety, forbids the use of 'any', blocks placeholder/incomplete code ("// TODOs"), and restricts the agent from jumping ahead to future features.
4. **Defines a 'Definition of Done' (DoD):** Gives a concrete verification test or compilation criteria for the user to confirm before moving to the next task.

---

### The Sprint Plan to Convert into Agent Prompts

#### Sprint 1: Local Infrastructure & Database Schema
- **Task 1.1: Multi-Container Setup & Environment Config**
  Create `docker-compose.yml` (postgres:16-alpine with volume, Next.js app placeholder), `.env.example`, and `.gitignore`.
- **Task 1.2: Prisma Schema & Declarative Models**
  Install prisma. Create `prisma/schema.prisma` mapping out `User`, `Project`, `Objective` (subtasks with order and isCompleted status), and `InventoryItem` models with proper cascading relations.

#### Sprint 2: Self-Hosted Authentication & Server Operations
- **Task 2.1: Local NextAuth.js Configuration**
  Create a globally cached Prisma client singleton (`src/lib/prisma.ts`). Set up the App Router NextAuth dynamic route handler utilizing the CredentialsProvider (email/password with bcryptjs) and the Prisma adapter.
- **Task 2.2: Next.js Server Actions (The Data Access Layer)**
  Create `src/app/actions/projects.ts` using the `"use server"` directive. Build out type-safe, authenticated actions authenticated with `getServerSession` and validated with `zod` for: `createProject`, `toggleObjective`, `updateInventoryQuantity`, and `getProjectsForUser`.

#### Sprint 3: UI Architecture & Client-Side Cache Store
- **Task 3.1: Component Library Scaffolding (shadcn style)**
  Initialize `src/lib/utils.ts` (clsx/tailwind-merge helper). Generate raw, styling-ready UI component primitives for: Button, Card, Checkbox, and Progress bar.
- **Task 3.2: Zustand Store & Front-End Pages Compilation**
  Create `src/store/useProjectStore.ts` to manage local layout state and handle client mutations. Build out `src/app/page.tsx` (Dashboard summary view) and the dynamic template `src/app/projects/[id]/page.tsx` (Project Workspace with checklists and ledger controls). Connect them to the Server Actions.

#### Sprint 4: Standalone Compilation & Production Containerization
- **Task 4.1: Production Dockerfile Optimization**
  Update `next.config.js` to output: 'standalone'. Build a high-performance multi-stage Dockerfile (`deps`, `builder`, `runner`). Add an entrypoint startup command that triggers `npx prisma migrate deploy` immediately before the node process starts.

---

Please review the blueprint above and generate the complete sequence of standalone agent prompts. Provide them clearly labeled so I can execute them step-by-step.